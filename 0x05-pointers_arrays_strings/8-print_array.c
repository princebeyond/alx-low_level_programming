#include <stdio.h>
#include "main.h"
/**
 * print_array - qqqawerty
 * @n: hghhg
 * @a: hddhdh
 */
void print_array(int *a, int n)
{
	int i;

	printf("%d", a[0]);

	for (i = 1; i < n; i++)
	{
		printf(", %d",a[i]);
	}
	printf("\n");
}
